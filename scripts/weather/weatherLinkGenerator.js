import API from "../Settings.js"


const parkWeatherData = {
    linkGenerator (parkData) {
        // let weatherLink = `https://api.openweathermap.org/data/2.5/forecast?lat=${}&lon=${}&units=imperial&appid=${API.weatherKey}`

        return weatherLink
    }
}

